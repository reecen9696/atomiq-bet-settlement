# Dependencies
node_modules/
.pnp
.pnp.js

# Rust
target/
**/*.rs.bk
# Keep Cargo.lock for applications (ensures reproducible builds)
# For libraries, you would want to ignore Cargo.lock

# Anchor
.anchor/
test-ledger/

# Testing
coverage/

# Production
dist/
build/

# Environment files (contain secrets like RPC URLs, API keys)
.env
.env.local
.env.*.local
# Keep .env.example as a template

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs (keep directory structure with .gitkeep)
logs/*.log
logs/*.pid
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Keypairs (NEVER commit! These contain private keys)
keys/*.json
keys/*.key
keys/*.keypair
**/id.json
**/phantom*.json
**/*keypair.json
**/*.keypair
# Keep keys directory structure with README

# Redis
dump.rdb
*.rdb

# Database
*.db
*.sqlite
*.sqlite3
